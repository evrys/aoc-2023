[package]
name = "hello-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
argmin = "0.8.1"
itertools = "0.12.0"
regex = "1.10.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
